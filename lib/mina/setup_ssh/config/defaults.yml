---
keys: []
add_command:
  - 'ssh-add'
  - '%<key_path>s'
sync_command:
  - 'rsync'
  - '-q'
  - '-e'
  - 'ssh -p %<port>s -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o LogLevel=quiet'
  - '%<key_path>s'
  - '%<user>s@%<domain>s:~/.ssh/%<key_name>s'
