---
keys: []
add_command:
  - 'ssh-add'
  - '%<key_path>s'
sync_command:
  - 'rsync'
  - '-ve'
  - '"ssh -p %<port>s -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no"'
  - '%<key_path>s'
  - '%<user>s@%<domain>s:~/.ssh/%<key_name>s'
